export type Dictionary = {
  id: string;
  title: string;
  slug: string;
  description: string;
  coverImageUrl?: string;
  published: boolean;
  createdAt: string;
  updatedAt: string;
  createdBy?: string;
};
