# Este archivo pisa el registry por defecto de ~/.npmrc (Mac del trabajo).
# No pongas tokens acá: se commitea al repo.

registry=https://registry.npmjs.org/

# pnpm: instalación predecible en monorepo
auto-install-peers=true
strict-peer-dependencies=false
